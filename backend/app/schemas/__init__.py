# Package: app.schemas
# Pydantic models used for API request validation and response serialisation.
# Every endpoint relies on these schemas to enforce type contracts and
# ensure consistent data shapes between the client and the server.

