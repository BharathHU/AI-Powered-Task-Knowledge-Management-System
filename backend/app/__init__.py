# Package: app
# Top-level application package for the AI Task & Knowledge Management API.
# Organises all submodules — API routes, core config, database, models,
# schemas, services — that together power the FastAPI backend.

