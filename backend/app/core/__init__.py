# Package: app.core
# Core infrastructure layer responsible for application configuration
# (env-based settings) and security primitives (JWT token creation,
# password hashing and verification). Referenced by every other layer.

