# Package: app.services
# Business-logic layer that encapsulates all domain operations.
# Each service module is consumed by one or more API route handlers
# and may depend on models, schemas, core utilities, and other services.

